info:
  name: send-message
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/chat/{{id}}/message"
  body:
    type: json
    data: |-
      {
        "content": "Second message"
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: 3d581826-e607-4ae6-8697-9ab7e9f0b033

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
