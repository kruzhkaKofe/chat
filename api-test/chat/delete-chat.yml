info:
  name: delete-chat
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/chat/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: 6bdf0ef1-6352-4cc8-8930-831cb0ee597c

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
